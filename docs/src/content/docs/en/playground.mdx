---
title: Playground
description: Simulate Aforro Série E and Série F cohorts directly in your browser — no install required.
---

import Playground from '../../../components/playground/Playground.tsx';

Choose Série E or Série F, set a subscription date, the number of units, and an "as of" date to see the live `SimulateResult` summary, the per-quarter schedule, and the mid-quarter accrued interest. The numbers are produced by the same `simulate()` function the npm package and CLI use — see the [Methodology](/en/methodology/) page for how each figure is derived.

<Playground locale="en" client:load />
